<!DOCTYPE html>
<html lang="en">
<head>
  <title>The Gossip Project</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  <style>
    .btn-facebook {
      background-color: #3b5998;
      color: white;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    .btn-facebook:hover {
      background-color: #2d4373;
    }
  </style>
</head>
<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">The Gossip Project</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/team">Équipe</a></li>
          <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
        </ul>
        <% if user_signed_in? %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Se déconnecter", session_path(current_user), method: :delete, class: "btn btn-danger" %>
            </li>
          </ul>
        <% else %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Se connecter", new_session_path, class: "btn btn-primary" %>
            </li>
          </ul>
        <% end %>
      </div>
    </div>
  </nav>
</header>

<div class="text-center mt-5">
  <h1>Bienvenue sur The Gossip Project !</h1>
  <p>Créez, lisez et interagissez avec des gossips passionnants.</p>
  <p>Rejoignez notre communauté et partagez vos histoires !</p>
  
  <% if user_signed_in? %>
    <%= link_to "Se déconnecter", session_path(current_user), method: :delete, class: "btn btn-facebook" %>
  <% else %>
    <%= link_to "Se connecter", new_session_path, class: "btn btn-facebook" %>
  <% end %>
</div>
